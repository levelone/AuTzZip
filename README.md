## AuTzZip - Australia Timezone Zipcodes

Install gem with command below

```ruby
gem install autzzip
```

Access console and run the ff:

```ruby
# require gem
require 'autzzip'

# find australian zipcode
Autzzip.find_by_zipcode('2000')
```
